# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment files — real values must never be committed
.env.local
.env.*.local

# Kubernetes Secret with real values — only secret.example.yaml is tracked.
k8s/secret.yaml

# Docker image tarball produced by k8s/apply.sh (docker save), regenerated each deploy.
feastbox-api.tar

# Personal demo reference (not part of the submission repo)
DEMO_RUNBOOK.txt
DEMO_COMMANDS.txt

# Deakin assessment task brief (local-only reference)
SIT226-7_2HD.md

# Project docs and AI tooling — never commit to remote
CLAUDE.md
docs_foundation/
docs/superpowers/
Plan_ORG/
.claude/
.mcp.json
*.pkl
feastbox-source-dump.txt

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
